import { SHARED_VERSION } from "@office/shared";

// Scaffold placeholder. Role shells (employee/staff/admin) land in later phases.
export function App() {
  return (
    <main style={{ fontFamily: "system-ui", padding: 24 }}>
      <h1>QuestionPro · Office Requests</h1>
      <p>Scaffold OK. @office/shared v{SHARED_VERSION}</p>
    </main>
  );
}
